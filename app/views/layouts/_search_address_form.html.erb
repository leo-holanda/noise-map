<%= form_with(url: "/search", method: "get") do %>
<p class="title">Procurar endereço</p>

  <div class="field has-addons">
    <div class="control">
      <%= text_field_tag 'address', nil, placeholder: 'e. g. Av. Silvio Carlos Viana, 2183', class: 'input' %>
    </div>

    <%= hidden_field_tag 'isComplaint', '0' %>

    <div class="field">
      <div class="control">
        <%= submit_tag("Procurar", class: 'button is-link') %>
      </div>
    </div>
  </div>
  
<% end %>